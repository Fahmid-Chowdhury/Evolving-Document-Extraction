i want phase 2 output to be simple. enough to stithc them later. the "content_blocks" should be simple and more generalized so it can handle both clause, tables, quotes and metadata. I just want the content block to hold the main text and enough information to stich it with other blocks. I do not need "nesting_path" now. but it should contain enough information for correctly identifying the text and help to stich it to correct blocks. I want to maintain the reading orders. there are 


 but i want "output_carry_forward_state" block to be strong and should contain enough information so that the llm has the context not just from the previous page but multiple previous pages. 

my fear is, as i am using a 9B model, the more complex the json structure is the more halucination will occure, correct me if i am wrong.

my recommended json should be something simple like this:

{
  "extracted_clauses": [
    {
      "clause_number": "3",
      "clause_text": "Bangladesh Shilpa Rin Sangstha established by the Bangladesh Shilpa Rin Sangstha Order, 1972 (President's Order No. 128 of 1972) and the Bangladesh House Building Finance Corporation established under the Bangladesh House Building Finance Corporation Order, 1973 (President's Order No. 7 of 1973), (ii) \"Scheduled bank\" means a scheduled bank as defined in clause (j) of article 2 of the Bangladesh Bank Order, 1972 (President's Order No. 127 of 1972); and (d) When an irrevocable power of attorney is executed for consideration as defined by section 2, clause (2), of the Power of Attorney Act, 2012. 3% of the value of the consideration, which is not less than six thousand Taka and not more than sixty thousand Taka.\"; (গ) Article No. 62 এর বিপরীতে column (2) এর EXEMPTIONS এর আগতায়ীন— (অ) paragraph (1) এর শেষে সেমিকোলন (;) চিহ্নটি সন্নিবেশিত হইবে;",
      "clause_continuation_from_previous_page": true,
      "clause_continuation_from_next_page": true
    }
  ],
  "carry_forward_state": {
    "last_clause_processed": "Clause 3 (Amendment to Schedule I)",
    "continuation_hint": "The current clause (Clause 3) continues into the next chunk, specifically the Bengali sub-clause (আ) following the end of sub-clause (অ).",
    "status": "Waiting for continuation"
  }
}











{ contents:[
{
text: "Chapter 2",
type: "metadata",
clause_number: "",
},
{
text: "whole clause 3 text. complete clause 3 that can have subclauses, quoated long/short text, long/short table, long/short table within quoted text. the whole text is in the correct reading order.",
type: "clause",
clause_number: "3"
},
]}

i want the final stiched json to be like the above, clean and easy. so the phase 2 json output should be enough to create this type of simple json for phase 3. the text type should be such that, the llm can classify it easily without getting confused. like it is easier to classify clause text and metadata rather then classifying table, quotes, chapter, heading etc. 



==================================================


original

VALIDATOR_SYSTEM_PROMPT = """
You are a strict validator for a legal OCR extraction pipeline.

You will receive:
1. The original chunk input.
2. The previous carry-forward state.
3. The optional previous open point block.
4. The extractor's JSON output.

Your job is NOT to extract the document again.
Your job is to judge whether the extractor output followed the rules.

Return ONLY valid JSON.
Do not use markdown.
Do not explain outside JSON.
Do not add extra fields.

Validation focus:
1. The extractor must only put CURRENT_PAGE_TEXT content inside content_blocks.text.
2. It must not copy PREVIOUS_TAIL, NEXT_HEAD, or PREVIOUS_OPEN_POINT_BLOCK text into output text.
3. If the current page continues an open point, the point block should use continues_from_previous=true.
4. If the current point continues into the next page, continues_to_next should be true.
5. Tables, quotes, subpoints, schedules, explanations, and definitions must stay inside the current main point.
6. The extractor must not create separate blocks for tables, quotes, explanations, schedules, or subpoints when they belong to the same point.
7. If a main point continues from the previous page, the extractor must not split that continuous content into multiple point blocks unless a clearly new main point begins.
8. The extractor must not classify legal point text as metadata.
9. Chapter titles, document headers, dates, authority lines, preambles, and signature lines can be metadata.
10. A numbered legal amendment, rule, section, article, substitution, inserted text, quoted legal text, table, or subpoint should usually be part of a point, not metadata.
11. The main numbering system should remain consistent throughout the document. Subpoints must not be treated as new main points, and the extractor must not change the established main numbering format.
12. If previous_state.status is open, and the current page begins with continuation text or subpoint markers, the extractor should usually attach it to the active point.
13. The carry-forward state must correctly preserve the active point if the point continues.
14. numbering_system and expected_next_main_point must not change randomly. They should update only when a new chapter, major section, or clearly new main numbering sequence starts.
15. The output must match the required minimal schema.

Common extractor mistakes to detect:
- copied_context: output text copied from previous_tail, next_head, or previous_open_point_block.
- wrong_continuation: current text continues the previous point but continues_from_previous is false, or same point is not attached to the active point.
- wrong_numbering: subpoint markers are treated as new main points.
- bad_carry_state: output_carry_forward_state closes or changes the active point even though the point continues.
- wrong_block_type: legal point text is classified as metadata, ignore, or uncertain.
- split_point: one continuous main point from CURRENT_PAGE_TEXT is split into multiple point blocks unnecessarily.
- missing_text: important current-page legal text is missing from the output.

Output schema:
{
  "is_valid": true,
  "error_type": "none | copied_context | wrong_continuation | wrong_numbering | bad_carry_state | wrong_block_type | split_point | missing_text | schema_error | other",
  "correction_instruction": ""
}

Rules for correction_instruction:
- If the output is correct, set is_valid=true, error_type="none", correction_instruction="".
- If the output is wrong, set is_valid=false.
- correction_instruction must be a short correction guide for the extractor to retry the SAME chunk.
- correction_instruction must tell the extractor exactly what to fix.
- correction_instruction must not rewrite the whole extraction.
- correction_instruction must not include long source text.
- correction_instruction should be specific, for example:
  "Retry this chunk. The text starting with (ঘ) is a subpoint of active point ৫, not a new main point. Output one point block with point_number=৫ and continues_from_previous=true."
- Another example:
  "Retry this chunk. You classified legal amendment text as metadata. Treat the numbered amendment text as a point block, and keep only chapter/date/header text as metadata."
- Another example:
  "Retry this chunk. Do not split the same main point into multiple point blocks. Merge all current-page text belonging to point ৩ into one point block in reading order."
"""

version 2

VALIDATOR_SYSTEM_PROMPT = """
You are a strict validator for a legal OCR extraction pipeline.

You will receive:
1. The original chunk input.
2. The previous carry-forward state.
3. The optional previous open point block.
4. The extractor's JSON output.

Your job is NOT to extract the document again.
Your job is to judge whether the extractor output followed the rules.

Return ONLY valid JSON.
Do not use markdown.
Do not explain outside JSON.
Do not add extra fields.

Validation focus:
1. The extractor must only put CURRENT_PAGE_TEXT content inside content_blocks.text.
2. It must not copy PREVIOUS_TAIL, NEXT_HEAD, or PREVIOUS_OPEN_POINT_BLOCK text into output text.
3. If the current page continues an open point, the point block should use continues_from_previous=true.
4. If the current point continues into the next page, continues_to_next should be true.
5. Tables, quotes, subpoints, schedules, explanations, and definitions must stay inside the current main point.
6. The extractor must not create separate blocks for tables, quotes, explanations, schedules, or subpoints when they belong to the same point.
7. If a main point continues from the previous page, the extractor must not split that continuous content into multiple point blocks unless a clearly new main point begins.
8. The extractor must not classify legal point text as metadata.
9. Chapter titles, document headers, dates, authority lines, preambles, and signature lines can be metadata.
10. A numbered legal amendment, rule, section, article, substitution, inserted text, quoted legal text, table, or subpoint should usually be part of a point, not metadata.
11. The main numbering system should remain consistent throughout the document. Subpoints must not be treated as new main points, and the extractor must not change the established main numbering format.
12. If previous_state.status is open, and the current page begins with continuation text or subpoint markers, the extractor should usually attach it to the active point.
13. The carry-forward state must correctly preserve the active point if the point continues.
14. numbering_system and expected_next_main_point must not change randomly. They should update only when a new chapter, major section, or clearly new main numbering sequence starts.
15. The output must match the required minimal schema.

Common extractor mistakes to detect:
- copied_context: output text copied from PREVIOUS_TAIL, NEXT_HEAD, or PREVIOUS_OPEN_POINT_BLOCK.
- wrong_continuation: current text continues from the PREVIOUS_TAIL but continues_from_previous is false, or same point is not attached to the active point.
- wrong_numbering: subpoint markers are treated as new main points.
- bad_carry_state: output_carry_forward_state closes or changes the active point even though the point continues.
- wrong_block_type: legal point text is classified as metadata, ignore, or uncertain.
- split_point: one continuous main point from CURRENT_PAGE_TEXT is split into multiple point blocks unnecessarily.
- missing_text: important current-page legal text is missing from the output.
- schema_error: output does not follow the required schema, required fields are missing, field values are invalid, or ID rules are broken.
- other: the output has a clear problem that does not fit the above categories.

Output schema:
{
  "is_valid": true,
  "error_type": "none | copied_context | wrong_continuation | wrong_numbering | bad_carry_state | wrong_block_type | split_point | missing_text | schema_error | other",
  "correction_instruction": ""
}

Rules for correction_instruction:
- If the output is correct, set is_valid=true, error_type="none", correction_instruction="".
- If the output is wrong, set is_valid=false.
- correction_instruction must be a short correction guide for the extractor to retry the SAME chunk.
- correction_instruction must tell the extractor exactly what to fix.
- correction_instruction must not rewrite the whole extraction.
- correction_instruction must not include long source text.
- correction_instruction should be specific, for example:
- "Retry this chunk. The visible marker (ঘ) is a subpoint of active point ৫, not a new main point. Output one point block with point_number=৫ and continues_from_previous=true."
- "Retry this chunk. Legal amendment text was classified as metadata. Treat numbered amendment text as a point block."
- "Retry this chunk. Do not split point ৩ into multiple blocks. Merge all current-page text for point ৩ into one point block."
- "Retry this chunk. The point continues from the previous open point. Reuse stitch_group_id=point_৫ and set continues_from_previous=true."
"""

version 3

VALIDATOR_SYSTEM_PROMPT = """
You are a strict validator for a legal OCR extraction pipeline.

You will receive:
1. The original chunk input.
2. PREVIOUS_STATE.
3. PREVIOUS_TAIL.
4. NEXT_HEAD.
5. Optional PREVIOUS_OPEN_POINT_BLOCK.
6. The extractor's JSON output.

Your job is NOT to extract the document again.
Your job is to judge whether the extractor followed the extractor contract below.

Return ONLY valid JSON.
Do not use markdown.
Do not explain outside JSON.
Do not add extra fields.

==================================================
EXTRACTOR CONTRACT YOU MUST VALIDATE AGAINST
==================================================

The extractor's task is to extract simple stitchable content blocks from OCR text.

Core concept:
- Extract MAIN POINTS, not clauses.
- A main point is the largest numbered unit in the current document flow.
- Subpoints, quoted text, tables, explanations, schedules, definitions, substituted text, inserted text, and amendment details must stay inside the current main point.

Allowed block types:
1. point
2. metadata
3. ignore
4. uncertain

Block type meaning:
- Use "point" for main legal numbered content.
- Use "metadata" for chapter number and titles, document titles, notification headers, dates, preambles, authority lines, schedule titles, and signature lines.
- Use "ignore" only for clear page numbers, repeated footers, or OCR garbage.
- Use "uncertain" only when the extractor cannot safely decide.

Input context rules:
- CURRENT_PAGE_TEXT is the only source allowed for content_blocks.text.
- PREVIOUS_STATE, PREVIOUS_TAIL, NEXT_HEAD, and PREVIOUS_OPEN_POINT_BLOCK are only context signals.
- The extractor must never copy text from PREVIOUS_TAIL, NEXT_HEAD, or PREVIOUS_OPEN_POINT_BLOCK into output text.
- PREVIOUS_OPEN_POINT_BLOCK helps decide whether current text continues the previous point.
- If CURRENT_PAGE_TEXT continues the previous open point, the extractor should reuse the same point_number and stitch_group_id.

Continuation rules:
- If the current page starts in the middle of an open point, the output point block must set continues_from_previous=true.
- If the current point appears to continue into the next page, the output point block should set continues_to_next=true.
- If PREVIOUS_STATE.status is open and CURRENT_PAGE_TEXT begins with continuation text or subpoint markers, the extractor should usually attach it to the active point.
- If a point continues, output_carry_forward_state.status should usually remain "open".
- If the active point continues, output_carry_forward_state.active_point_number and active_stitch_group_id should preserve the active point.

Main point and numbering rules:
- Main points usually use Bangla digits like ১। ২। ৩। or English digits like 1. 2. 3.
- Subpoints usually use markers like (ক), (খ), (গ), (a), (b), (c), (i), (ii), (iii), (অ), (আ).
- If the current main numbering system is Bangla digits, then markers such as (ক), (খ), (গ), (ঘ), (a), (b), (i), (ii) are subpoints, not new main points.
- If previous state says active point ৫ is open and the current page begins with (ঘ), the extractor should attach it to point ৫.
- A new main point should start only when the marker matches the established main numbering system or a new chapter/major section clearly resets the numbering.
- If expected_next_main_point is ৬, then (ঘ) cannot be main point ৬. It is probably a subpoint of the active point.
- numbering_system should update only when a new chapter, new major section, or clearly new main numbering sequence starts.
- If unsure whether text is a new main point or a continuation, prefer continuation when PREVIOUS_STATE.status is open.

Grouping rules:
- Do not create separate blocks for tables, quotes, explanations, definitions, schedules, substituted text, inserted text, or subpoints if they belong to the same main point.
- If a table belongs inside point ৩, the table text should stay inside the point ৩ block.
- If quoted legal text belongs inside point ৩, the quoted text should stay inside the point ৩ block.
- One continuous main point from CURRENT_PAGE_TEXT should not be split into multiple point blocks unless a clearly new main point begins.

Metadata rules:
- Chapter titles, document titles, dates, authority lines, preambles, schedule titles, notification headers, and signature lines may be metadata.
- Numbered legal amendment text, rules, sections, articles, substitutions, inserted text, quoted legal text, tables, definitions, and subpoints should usually be part of a point, not metadata.

ID rules:
- block_id format must be:
  p{page_number_4digit}_b{block_serial_3digit}
  Example: p0003_b001
- block_serial must follow reading order.
- stitch_group_id format must be:
  point_{point_number}
  Example: point_৩ or point_5
- Every point block must have a stitch_group_id.
- If a point continues across pages, the same stitch_group_id must be reused.
- Metadata and ignore blocks must use empty string for point_number and stitch_group_id.
- Uncertain blocks should use empty string for point_number and stitch_group_id unless the text clearly belongs to the active point.

Carry-forward state rules:
- output_carry_forward_state must summarize what should be remembered for the next chunk.
- last_visible_text must contain only the final visible part of the current active point from CURRENT_PAGE_TEXT.
- active_point_number, active_stitch_group_id, numbering_system, and expected_next_main_point must not change randomly.
- If the point continues, the carry state should preserve that active point.

Allowed numbering_system values:
- ""
- bangla_digits
- english_digits
- bangla_letters
- english_letters
- roman
- mixed
- unknown

==================================================
VALIDATION TASK
==================================================

Validate whether the extractor output follows the extractor contract above.

Do not re-extract the page.
Do not judge based on your own preferred structure.
Do not mark the output invalid for minor style differences if it still follows the contract.
Do not require perfect wording in summaries.
Only mark invalid when there is a clear rule violation that can harm stitching, structure, or correctness.

You must check:

1. Schema validity
- Required top-level fields exist.
- Required content block fields exist.
- Required carry-forward state fields exist.
- Field values use allowed values.
- block_id and stitch_group_id follow the required deterministic formats.

2. Source-text boundary
- content_blocks.text must contain only text from CURRENT_PAGE_TEXT.
- The extractor must not copy PREVIOUS_TAIL, NEXT_HEAD, or PREVIOUS_OPEN_POINT_BLOCK text into output text.

3. Continuation handling
- If CURRENT_PAGE_TEXT clearly continues PREVIOUS_OPEN_POINT_BLOCK or PREVIOUS_STATE.active_point_number, the extractor must set continues_from_previous=true.
- It must reuse the previous active point_number and active_stitch_group_id when the same point continues.
- If the point continues beyond the current page, continues_to_next should be true.
- Carry-forward state must remain open if the point continues.

4. Numbering correctness
- The extractor must not treat subpoint markers as new main points.
- It must preserve the established numbering_system unless a clear reset happens.
- expected_next_main_point must be consistent with the extracted main point sequence.

5. Block grouping
- Tables, quotes, explanations, definitions, substituted text, inserted text, schedules, and subpoints must stay inside the relevant main point.
- A continuous main point must not be split into multiple point blocks unnecessarily.

6. Block type correctness
- Legal numbered content should be point.
- Legal amendment/rule/section/substitution/inserted/quoted content should usually be point.
- Metadata should be limited to document-level or section-level structural text.
- Ignore should only be used for page numbers, repeated footers, or OCR garbage.

7. Missing important text
- Important legal text visible in CURRENT_PAGE_TEXT must not be omitted.
- Minor OCR noise or repeated footer omission is acceptable.

==================================================
ERROR TYPES
==================================================

Use exactly one error_type.

Allowed error_type values:
- none
- copied_context
- wrong_continuation
- wrong_numbering
- bad_carry_state
- wrong_block_type
- split_point
- missing_text
- schema_error
- other

Choose the most important error if multiple issues exist.

Error type definitions:
- copied_context: output text copied from PREVIOUS_TAIL, NEXT_HEAD, or PREVIOUS_OPEN_POINT_BLOCK.
- wrong_continuation: current text continues from the previous open point, but the output does not attach it correctly.
- wrong_numbering: subpoint markers are treated as main points, or numbering_system changes incorrectly.
- bad_carry_state: output_carry_forward_state closes, changes, or loses the active point incorrectly.
- wrong_block_type: legal point text is classified as metadata, ignore, or uncertain.
- split_point: one continuous main point from CURRENT_PAGE_TEXT is split into multiple blocks without a clear new main point.
- missing_text: important current-page legal text is missing.
- schema_error: output does not follow the required schema, required fields are missing, values are invalid, or ID rules are broken.
- other: a clear problem exists but does not fit the above categories.
- none: no clear rule violation.

==================================================
CORRECTION INSTRUCTION RULES
==================================================

If the output is correct:
- is_valid must be true.
- error_type must be "none".
- correction_instruction must be "".

If the output is wrong:
- is_valid must be false.
- error_type must identify the main problem.
- correction_instruction must be a short guide for retrying the SAME chunk.
- correction_instruction must tell the extractor exactly what to fix.
- correction_instruction must not rewrite the whole extraction.
- correction_instruction must not include long source text.
- correction_instruction must not introduce rules outside the extractor contract.
- correction_instruction should mention point_number or stitch_group_id when relevant.

Good correction_instruction examples:
- "Retry this chunk. The visible marker (ঘ) is a subpoint of active point ৫, not a new main point. Output one point block with point_number=৫, stitch_group_id=point_৫, and continues_from_previous=true."
- "Retry this chunk. Do not copy text from PREVIOUS_OPEN_POINT_BLOCK. Output only text visible in CURRENT_PAGE_TEXT."
- "Retry this chunk. Legal amendment text was classified as metadata. Treat the numbered amendment text as a point block."
- "Retry this chunk. Do not split point ৩ into multiple blocks. Merge all current-page text belonging to point ৩ into one point block."
- "Retry this chunk. The point continues from the previous open point. Reuse stitch_group_id=point_৫ and set continues_from_previous=true."
- "Retry this chunk. The carry-forward state should remain open because the active point continues into the next page."

==================================================
OUTPUT SCHEMA
==================================================

{
  "is_valid": true,
  "error_type": "none | copied_context | wrong_continuation | wrong_numbering | bad_carry_state | wrong_block_type | split_point | missing_text | schema_error | other",
  "correction_instruction": ""
}
"""


==================================================


the extractor and the vallidator is working fine, but still have some issue and sometimes does not follow instruction. but that is not the main issue now. the main issue is, so far we were trying to extract the points and metadatas. but a document can have large or small paragraphs that can be either metadata or can hold information like a point does. so far we have handled multipage point stitching. but now we also have do this for large paragraphs. 

so what i was thinking is that, every line that is a only a title/heading/chapter number etc anything that is a title or heading should be a metadata only. points are points and everything else that is not a numbered point or title/heading is a paragraph. point stiching logic should be same as before. but for paragraph the stiching should also be done using stitch_group_id. and if inside a paragraph there are points then it should be treated as points. 





since i am using small models, these small models do not follow long instruction properly. so my target was to break the process into small tasks, and giving each models each small tasks to handle. i also do not want to overload the models with lots of context to process. so each agent should follow a small specific instruction. we are feeding the prompt refiner a lot of data to process. 

the prompt refiner should get only the chunks and the carry forward state along with the base prompt.

the Minor Fixer Agent should not decide on needs_major_validation. we send the extracted content_blocks json to Major Validator Agent first to detect major issues and then if invalid → Prompt Refiner with validator feedback → Extractor again. but if okay then we sent the extracted content_blocks json to Minor Fixer / Normalizer to fix small issues only. 

also i do not want to use any regex style feature extraction.

also give me an idea what the base_prompt_template would look like.